# Relationship to Asahi

The name `hinode` was inspired by `asahi`.

- `asahi`: morning sun / rising sun
- `hinode`: sunrise

This project may use Asahi Linux as one useful reference or test environment,
but it is not intended to be limited to Asahi Linux or Apple Silicon.

The intended scope is generic AArch64 UEFI.
